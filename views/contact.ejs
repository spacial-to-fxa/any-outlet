<%- include('layout', { body: `
    <h2>Contact Us</h2>
    <p><strong>Shop:</strong> ${config.shopName}</p>
    <p><strong>Address:</strong> ${config.address || '-'}</p>
    <p><strong>Phone:</strong> ${config.phone || '-'}</p>
    <p><strong>Line ID:</strong> ${config.lineId || '-'}</p>
` }) %>